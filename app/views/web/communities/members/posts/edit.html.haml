- title 'Edit post'
- if @post.active?
  %p= link_to 'view on site', community_blog_post_path(@post.blog, @post)
= simple_form_for @post, :url => community_member_post_path(member, @post) do |f|
  = render 'form', :f => f

